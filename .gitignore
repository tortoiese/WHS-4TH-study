# 컴파일 산출물 (바이너리) - git에 올리지 않음
*.exe
*.o
*.obj
*.out

# 디버그/캐시
*.pdb
*.ilk

# IDA Pro 데이터베이스/분석 산출물 (원본 exe로 재생성 가능, 용량 큼 → 제외)
*.i64
*.idb
*.id0
*.id1
*.id2
*.nam
*.til

# 컴파일 중간 결과물(.s 어셈블리, .i 전처리)은 학습용으로 유지하려면 아래 두 줄을 주석 처리(#)된 상태로 둔다.
# 제외하고 싶으면 주석 해제:
# *.s
# *.i
